/* const { styleClass } = require('../../utils');

module.exports = (root, opts) => {
    const userSelect = {
        'select-none': 'none',
        'select-text': 'text',
        'select-all': 'all',
        'select-auto': 'auto'
    };

    styleClass('user-select', userSelect, root, opts);
};
 */
