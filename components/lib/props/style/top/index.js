/* const { styleClass } = require('../../utils');

module.exports = (root, opts) => {
    const top = {
        'top-auto': 'auto',
        'top-0': '0px',
        'top-50': '50%',
        'top-100': '100%'
    };

    styleClass('top', top, root, opts, true);
};
 */
