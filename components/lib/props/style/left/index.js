/* const { styleClass } = require('../../utils');

module.exports = (root, opts) => {
    const left = {
        'left-auto': 'auto',
        'left-0': '0px',
        'left-50': '50%',
        'left-100': '100%'
    };

    styleClass('left', left, root, opts, true);
};
 */
