/* const { styleClass } = require('../../utils');

module.exports = (root, opts) => {
    const whiteSpaces = {
        'white-space-normal': 'normal',
        'white-space-nowrap': 'nowrap'
    };

    styleClass('white-space', whiteSpaces, root, opts, true);
};
 */
