/* const addFixedSize = (obj, prefix, opts) => {
    for (let i = 1; i <= opts.fixedRemLimit.width; i++) {
        obj[prefix + i + 'rem'] = i + 'rem';
    }
};

module.exports = {
    addFixedSize
};
 */
