/* const color = require('./color');
const filter = require('./filter');
const gap = require('./gap');
const size = require('./size');

module.exports = {
    color,
    filter,
    gap,
    size
};
 */
