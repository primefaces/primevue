/* const align = require('./properties/align');
const decoration = require('./properties/decoration');
const overflow = require('./properties/overflow');
const transform = require('./properties/transform');

module.exports = (root, opts) => {
    // properties
    align(root, opts);
    decoration(root, opts);
    overflow(root, opts);
    transform(root, opts);
};
 */
