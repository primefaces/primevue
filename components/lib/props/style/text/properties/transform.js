/* const { styleClass } = require('../../../utils');

module.exports = (root, opts) => {
    const textTransforms = {
        lowercase: 'lowercase',
        uppercase: 'uppercase',
        capitalize: 'capitalize'
    };

    styleClass('text-transform', textTransforms, root, opts, true);
};
 */
