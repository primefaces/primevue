/* const { styleClass } = require('../../../utils');

module.exports = (root, opts) => {
    const textDecorations = {
        underline: 'underline',
        'line-through': 'line-through',
        'no-underline': 'none'
    };

    styleClass('text-decoration', textDecorations, root, opts, true, true);
};
 */
