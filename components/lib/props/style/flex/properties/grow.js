/* const { styleClass } = require('../../../utils');

module.exports = (root, opts) => {
    const flexGrow = {
        'flex-grow-0': 0,
        'flex-grow-1': 1
    };

    styleClass('flex-grow', flexGrow, root, opts, true);
};
 */
