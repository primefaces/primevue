/* const { styleClass } = require('../../utils');

module.exports = (root, opts) => {
    const right = {
        'right-auto': 'auto',
        'right-0': '0px',
        'right-50': '50%',
        'right-100': '100%'
    };

    styleClass('right', right, root, opts, true);
};
 */
