import BaseStyle from '@primevue/core/base/style';

const theme = ({ dt }) => `
`;

const classes = {
    root: 'p-iftalabel'
};

export default BaseStyle.extend({
    name: 'iftalabel',
    theme,
    classes
});
